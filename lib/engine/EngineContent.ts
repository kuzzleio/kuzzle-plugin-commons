/**
 * Represents the content of the engine configuration document
 */
export type EngineContent = {
  /**
   * Engine index
   */
  index: string;

  /**
   * Engine group
   */
  group: string;

  /**
   * Plugin name
   */
  name: string;
};